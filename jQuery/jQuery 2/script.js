$(document).ready(
    function(){
        $('#menu').click(
            function() {
                // fadeToggle=fadeIn+fadeOut
                // $('.category').fadeToggle(500);
                // slideToggle=slideDown+slideUp
                $('.category').slideToggle(500);
                // toggle
                // $('.category').toggle(500);

            }
        );
    }
);